1. What is the difference between date and Timedatectl?

There is one important difference: **When you set a time with the date command then only system time is changed.** **But if you set a time with the timedatectl then both system time and default rtc time is affected**.

